<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.restaurant.user.question.dao.QuestionDAO">

	<!-- 1:1 문의 등록 -->
	<insert id="questionInsert" parameterType="question">
		<selectKey keyProperty="question_no" resultType="int" order="BEFORE">
			select question_seq.nextval from dual
		</selectKey>
		insert into question(question_no, member_no, question_type,
		question_title, question_date, question_text, question_file,
		question_process)
		values(#{question_no}, #{member_no},
		#{question_type}, #{question_title}, sysdate, #{question_text},
		#{question_file}, '대기중')
	</insert>

</mapper>